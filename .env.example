# ── Frontend (client/.env) ─────────────────────────────────────────────────
VITE_MAPBOX_TOKEN=your_mapbox_public_token_here
VITE_API_URL=http://localhost:5001

# ── Backend (server/.env or Railway env vars) ──────────────────────────────
PORT=5001
FLASK_DEBUG=false
